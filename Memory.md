# Memory.md — Project Memory Log

**Purpose:** This file is the AI's persistent working memory for this repo. It must be read at the start of every session and updated at the end of every session (or whenever significant work is completed), so no context is lost across breaks, queues, or new sessions — and so the AI never hallucinates prior progress.

**Rule for any AI working on this project:** Before writing any code, read this file fully. Before ending a session (or when told the session is ending), update the sections below with exactly what changed. Do not mark something "Completed" unless it actually works end-to-end per the checklist in Rules.md §4.

---

## 1. Current Status

**Current phase (per Phases.md):** _Not started — Phase 0_
**Currently active file/feature:** _None yet_
**Last updated:** _(update this timestamp every time this file changes)_

---

## 2. What Has Been Completed

_(Append entries here — never delete history, only add. Newest at the top.)_

```
- [date] Nothing yet. Project scaffolding (PRD, Architecture, Rules, Phases, Design, Security docs) created.
```

---

## 3. Currently In Progress

_(What file(s)/feature(s) are actively being worked on right now, and the exact next step. Overwrite this section each session — it should always reflect the live state.)_

```
File: —
Task: —
Next step: —
Blockers: —
```

---

## 4. Key Decisions Log

_(Any decision that isn't obvious from the docs alone — e.g. "chose Prisma over Drizzle because X", "chose Clerk over Supabase Auth because Y". Append only.)_

```
- [date] (none yet)
```

---

## 5. Known Issues / Deferred Items

_(Anything intentionally left broken/incomplete/deferred, and why — so a future session doesn't "fix" something that was deliberately postponed, or waste time rediscovering a known bug.)_

```
- (none yet)
```

---

## 6. Session Handoff Notes

_(Free-form: anything the next session needs to know that doesn't fit above — e.g. "Design.md theme decision still open, don't hard-code colors yet.")_

```
- Design.md theme (metallic vs flat dark) is not finalized — do not hard-code final color tokens until decided.
- Tech stack choices (ORM, auth provider) should be confirmed and recorded in Architecture.md + this log before Phase 0 work begins.
```